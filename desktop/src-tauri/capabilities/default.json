{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the AIUsageBar usage panel.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
